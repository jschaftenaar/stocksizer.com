import React from 'react';
import style from './Header.module.scss';

const Header = () => (
  <div className={style.header}>
    <h1 className={style.title}>Stock Sizer</h1>
  </div>
);

export default Header;
